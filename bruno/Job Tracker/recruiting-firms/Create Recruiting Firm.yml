info:
  name: Create Recruiting Firm
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/recruiting-firms"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
