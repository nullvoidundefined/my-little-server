info:
  name: Get All Recruiters
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/recruiters"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
